package xyz.goldendupe.database;

public class SpawnDatabase {
	private final String CREATE_TABLE = "CREATE TABLE IF NOT EXISTS spawn (name VARCHAR(10), x DOUBLE, y DOUBLE, z DOUBLE, pitch DOUBLE, yaw DOUBLE)";
}
